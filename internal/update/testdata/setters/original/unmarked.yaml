apiVersion: apps/v1
kind: Deployment
metadata:
  name: foo
  namespace: bar
spec:
  template:
    spec:
      containers:
      - name: c
        image: image:v1.0.0 # uses the same image. but should be left alone
